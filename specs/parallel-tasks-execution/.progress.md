# Progress: parallel-tasks-execution

## Goal
Tasks in tasks.md should be marked as possible for parallel execution when they can run concurrently, and the root/coordinator agent must run parallelizable tasks in parallel (similar to how research and explore phases already use parallel teammates).

## Goal Type
- Type: add
- Keywords: add, implement, parallel

## Phases
- [x] Research
- [x] Requirements
- [ ] Design
- [ ] Tasks
- [ ] Execution

## Interview Responses

### Requirements Phase (quick mode)
- User stories: 6 (task author marking, coordinator detection, dispatch, isolation, merging, observability)
- Functional requirements: 10 (format, detection, dispatch, isolation, merging, dependencies, fault tolerance, state tracking, backward compatibility, pattern replication)
- Non-functional requirements: 5 (performance, no breaking changes, backward compatibility, code simplicity, observability)
- Chosen approach: Full parallel pattern replication — implement coordinator to dispatch parallelizable tasks all in one message, with isolated progress files and result merging (mirrors existing research phase)

## Completed Tasks
- [x] 1.1 Add [P] marker rules section to task-planner agent
- [x] 1.2 Add [P] heuristics to task-planner agent
- [x] 1.3 Add [P] to POC task examples in task-planner agent
- [x] 1.4 [VERIFY] Quality checkpoint: verify task-planner.md changes - PASS (13 [P] occurrences)
- [x] 1.5 Add [P] examples to POC section in tasks template
- [x] 1.6 Add [P] examples to TDD section in tasks template

## Current Task
Awaiting next task

## Learnings
- Most parallel execution infrastructure already exists: coordinator-pattern.md has [P] detection, parallel group building, team-based dispatch, progress merge, batch state update, and failure handling
- spec-executor.md already supports progressFile parameter, flock-based locking for tasks.md and git operations
- Only 3 files need actual changes: task-planner.md (add [P] rules), templates/tasks.md (add [P] examples), stop-watcher.sh (parallel group resume)
- [VERIFY] tasks always break parallel groups -- quality checkpoints are sequential barriers
- Phase boundaries (1.N -> 2.1) also break parallel groups
- Max practical group size is ~5 concurrent Task() calls
- TeamCreate fallback already documented: if team creation fails, direct Task() calls in one message still achieve parallelism
- Task planning: 43 tasks across 5 phases (POC-first workflow chosen since this adds a new capability)
- Task planning: stop-watcher changes must be sequential (1.9 before 1.10, 1.12 before 1.13) because each builds on prior additions
- Task planning: task-planner.md and templates/tasks.md changes are independent [P] groups -- good dogfooding opportunity
- Task planning: version bumps (plugin.json + marketplace.json) are independent [P] tasks
- Task planning: no test runner exists for this plugin; all verification via grep/bash checks
- Task planning: bash -n is the only syntax validation available for stop-watcher.sh
