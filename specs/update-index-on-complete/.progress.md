# Progress: update-index-on-complete

## Goal
We need to make sure to have a hook that if all tasks are completed, the index state (if we have it) should have the current spec we worked on.

## Goal Type
- Classification: add
- Intent: Implement a hook that updates the spec index with the completed spec after all tasks finish

## Skill Discovery
- **Hook Development** (.agents/skills): matched (keyword: hook)
- **Command Development** (.agents/skills): no match
- **Plugin Structure** (.agents/skills): no match
- **Plugin Settings** (.agents/skills): no match
- **MCP Integration** (.agents/skills): no match
- **Skill Development** (.agents/skills): no match

### Post-Research Retry
- No new skills matched

## Completed Tasks
- [x] 1.1 Add index update to primary ALL_TASKS_COMPLETE detection path
- [x] 1.2 Add index update to fallback ALL_TASKS_COMPLETE detection path
- [x] 4.1 Bump plugin version to 4.5.0 in plugin.json and marketplace.json
- [x] 5.1 Create PR - PR #111

## Current Task
Awaiting next task

## Learnings
- Two separate ALL_TASKS_COMPLETE detection paths exist in stop-watcher.sh (primary at line 70, fallback at line 77) -- both need the index update call
- The "all tasks verified complete" path (line 183-185) is a different exit: state says complete AND tasks.md confirms. This path does NOT need the update because the coordinator should have already run it. Only the transcript-based detection is unreliable.
- $SCRIPT_DIR is already set and used for path-resolver.sh sourcing, so reusing it for update-spec-index.sh is consistent
- update-spec-index.sh sources path-resolver.sh itself, so it works standalone -- no dependency on stop-watcher having sourced it first
- Design confirmed: only 2 lines needed, one per ALL_TASKS_COMPLETE detection branch. No new components, no abstractions.
- Error suppression pattern `2>/dev/null || true` is already used throughout stop-watcher.sh -- consistent approach
- Task planning: minimal 7-task plan (2 impl + 1 verify + 1 version bump + 1 verify + 2 PR). No TDD since shell scripts have no test framework in this project.
- Version bump: 4.4.0 -> 4.5.0 (minor, new feature). Both plugin.json and marketplace.json must match.
- No VE tasks needed: this is a hook script modification, not a runnable app/library. Verification is via grep/syntax checks.
- Task 1.1 verify command uses -A2 but needs -A3 due to 2 comment lines between echo and the new line. Implementation is correct; verify grep context was just one line short.

### Verification: 1.3 [VERIFY] Verify both paths have index update and script structure is valid
- Status: PASS
- bash -n stop-watcher.sh: exit 0 (valid syntax)
- update-spec-index.sh --quiet call count: 2 (line 74 primary, line 80 fallback)
- $SCRIPT_DIR path resolution: confirmed on both calls
- Error suppression (2>/dev/null || true): confirmed on both calls
- Full verify command: PASS

### Verification: 4.2 [VERIFY] Final verification - script syntax + AC checklist
- Status: PASS
- bash -n stop-watcher.sh: exit 0 (valid syntax)
- AC-1.1: PASS -- Primary path (line 74) calls `update-spec-index.sh --quiet` before `exit 0` on line 75
- AC-1.2: PASS -- Both calls use `2>/dev/null || true` (non-blocking, errors suppressed)
- AC-1.3: PASS -- Both detection paths have the call (line 74 primary, line 80 fallback)
- AC-2.1: PASS -- Primary path exits 0 (line 75), fallback path exits 0 (line 81), no change to exit behavior
- AC-2.2: PASS -- implement.md Section 10 still contains `update-spec-index.sh --quiet` at line 171 (defense in depth preserved)
- AC-2.3: SKIP -- Runtime performance not measurable in static check; update-spec-index.sh is a fast script
- Version check: PASS -- Both plugin.json and marketplace.json show "4.5.0"
- Combined verify command: ALL_AC_PASS

### Verification: 5.2 [VERIFY] CI pipeline passes
- Status: PASS
- gh pr checks 111: All 4 checks passed
  - Run bats tests (workflow 22634880644): pass (1m38s)
  - Run bats tests (workflow 22634899762): pass (1m37s)
  - Verify .current-spec not committed: pass (4s)
  - Verify plugin version bump: pass (5s)
- PR #111 state: OPEN, mergeable: MERGEABLE

## Next
Awaiting next task
