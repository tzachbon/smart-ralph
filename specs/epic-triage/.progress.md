# Epic Triage Feature

## Original Goal
Add /ralph-specum:triage command for decomposing large features into multiple dependency-aware specs (epics), and enhance /start with epic awareness.

## Current Goal
Implement the epic triage feature per the design at docs/plans/2026-03-03-epic-triage-design.md and plan at docs/plans/2026-03-03-epic-triage-plan.md.

## Completed Tasks
- [x] 1.1 Add epic state schema definition - 9119813
- [x] 1.2 Create epic.md template - 22c92e4
- [x] 1.3 Create triage-analyst agent
- [x] 1.4 Create triage-flow reference
- [x] 1.5 Create triage.md command
- [x] 1.6 Add epic detection to start.md
- [x] 1.7 Add epic state update to stop-watcher.sh
- [x] 2.2 Update CLAUDE.md documentation
- [x] 2.3 Update plugin description and keywords

## Learnings
- Schema file uses 2-space indentation consistently
- `epicState` added as sibling to `state` and `task` definitions
- `epicName` added as last property in `state.properties`
- Existing templates use `---` frontmatter with key-value pairs and `{{UPPER_CASE}}` for frontmatter, `{{description}}` for content placeholders
- Agent frontmatter follows pattern: name, description, model: inherit, color: <color>
- Existing agents use `<mandatory>` tags for enforced sections and consistent Communication Style block
- Command files use `---` YAML frontmatter with description, argument-hint, allowed-tools fields
- Commands follow pattern: Checklist (numbered), Step sections, `<mandatory>` blocks for delegation and walkthrough requirements
- Reference files are included via `${CLAUDE_PLUGIN_ROOT}/references/` pattern
- Reference files style: `# Title` line 1, blank line, `> Used by: <command>.md` line 3, then `## Section` headings
- start.md uses numbered steps (Step 1, 2, 3, 4, 5) with Step 3.5 inserted for epic detection between Scan Existing Specs and Route to Action

### Verification: 2.1 [VERIFY] Verify all new files and modifications
- Status: PASS
- Check 1 (4 new files exist): PASS - all 4 files found
- Check 2 (schema valid JSON): PASS - jq empty succeeded
- Check 3a (epicState.required): PASS - outputs ["name","goal","specs"]
- Check 3b (epicName type): PASS - outputs "string"
- Check 4 (stop-watcher valid bash): PASS - bash -n succeeded
- Check 5 (stop-watcher epic support): PASS - epicName references found
- Check 6 (start.md Epic Detection): PASS - "Epic Detection" heading found
- Check 7 (start.md epicName): PASS - epicName references found
- Check 8 (start.md current-epic gitignore): PASS - current-epic reference found
- Check 9 (triage.md frontmatter): PASS - has description, argument-hint, allowed-tools
- Check 10 (triage-analyst.md frontmatter): PASS - has name, description, model, color

## Blockers
(none)

## Current Task
Awaiting next task

## Next
Task 2.4 Version bump
