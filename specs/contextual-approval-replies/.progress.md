# Progress: contextual-approval-replies

## Original Goal

Implement GitHub issue #148 across both `ralph-specum` and `ralph-specum-codex`: accept contextual natural-language replies at approval gates. Infer an unambiguous affirmative only when one explicit action is pending; preserve auditability and existing state, manifest, and delegation checks.

Source: https://github.com/tzachbon/smart-ralph/issues/148

## Scope Envelope

- Target: Contextual approval handling in both shipped Ralph Specum plugin surfaces and their existing Bats coverage.
- Action: Modify the shared gate/schema, relevant approval-gate contracts, tests, and required local version metadata; commit locally only.
- Bounds: Limit changes to the files named by this spec's approved tasks. No new dependencies, NLP, unrelated refactors, remote writes, pushes, PRs, merges, or issue updates.
- Deliverable: A local implementation of issue #148 with deterministic one-action resolution, auditability, copy parity, and passing local verification.
- Complete when: The focused and full Bats suites, helper/schema parity checks, JSON validation, and whitespace check pass at version 4.12.1.
- Escalate when: Completion requires a behavior outside the one-pending-action rule, a file outside the approved task scope, a new dependency, or any remote action.

## Status

- Phase: completed
- Mode: quick (`--quick`)
- Next: local workflow complete.

## Goal Context

- Outcome: an unambiguous affirmative reply authorizes the one pending approval action.
- Scope: both plugin surfaces; interview delegation, artifact advancement, and recorded revision authorization.
- Safety: do not infer approval for questions, revisions, unrelated text, or multiple pending actions; retain the original reply and normalized action for audit.
- Approach: extend the shared gate path instead of adding disconnected phrase checks at individual prompts.

## Related Specs

- `adaptive-interview` and `goal-interview` contain prior interview-flow work to inspect during research.

## Skill Discovery

- Pass 1 (`rev-1`): selected `interview-framework-codex` (required core), `ralph-specum-start` (explicit invocation), and `ponytail` (active coding constraint).

## Blockers

- None. User confirmed both plugin surfaces and exact quick mode.

## Learnings

- Both `plugins/ralph-specum*/scripts/phase_gate.py` copies currently classify affirmative replies as control-only, so the shared gate behavior is the smallest likely implementation seam.
- The helper and schema copies are byte-identical and Bats asserts that parity; update them together rather than creating surface-specific resolvers.
- Pre-delegation already has one authoritative pending confirmation ID, but artifact approval has only `awaitingApproval`; a persisted one-action descriptor is needed for safe contextual resolution and resume.
- Keep `classify-reply` unchanged for active interview questions. Resolve contextual approval only at an explicitly persisted single-action gate, then retain the existing confirm, manifest, delegation, and writer checks.
- No new dependency is needed. Focused Bats coverage should exercise affirmative acceptance, question/quoted/revision rejection, multi-action no-op, audit fields, and resume.
- Requirements scope is limited to one state-aware resolver/audit path: pre-delegation uses its pending confirmation ID; artifact advancement and revision dispatch require one persisted descriptor, with revision feedback already recorded.
- Observable Bats coverage must prove accepted affirmatives, every fail-closed rejection class, audit fields, resume behavior, helper/schema parity, and applicable release metadata updates.
- Design selects one `resolve-approval` helper command, an optional one-object `approvalGate`, and helper-owned append-only `approvalAudit`; no NLP or alternate delegation path is needed.
- A descriptor must match the current phase and `awaitingApproval`; revision descriptors carry recorded feedback, while multi-option artifact prompts intentionally persist no descriptor and require clarification.
- The mandatory quick prototype was reviewed and published as `skipped` with `sourceDisposition: not_created`; it contributes no downstream evidence or source changes.
- The fine-grained POC-first plan has 18 local-only tasks. It starts with the shared schema/helper, then aligns only the relevant approval contracts, adds Bats coverage, and finishes with release metadata plus the full local suite.
- The fine-grained POC-first task plan has 18 local-only tasks: shared schema/resolver first, then narrowly aligned Claude/Codex handoffs, Bats coverage, metadata, and full local quality checks. It intentionally omits push, PR, CI polling, merge, and issue-mutation work for exact quick mode.
- Task 3.2's required Codex 4.12.1 expectation depends on the release-metadata task, so task 4.1 was moved ahead of 3.2 as its explicit prerequisite; task count remains 18.
- Full V6 uncovered one additional stale 4.12.0 assertion in `tests/interview-framework.bats`; added focused task 4.2 before rerunning V6, raising the plan count to 19.
- Verification V1: FAIL — `bats tests/phase-gates.bats` could not start because `bats` is not installed (`/bin/bash: bats: command not found`, exit 127); parity checks were not reached.
- Verification V1 retry: PASS — `bats tests/phase-gates.bats` passed 29/29 (exit 0); both helper and schema parity checks passed (exit 0).
- Verification V3: PASS — `bats tests/claude-phase-flow.bats` passed 25/25 (exit 0), and all six Claude phase commands reference `resolve-approval` or `normal-mode-gates.md` (exit 0).
- Verification V5: PASS — `bats tests/phase-gates.bats tests/claude-phase-flow.bats tests/codex-phase-flow.bats` passed 74/74 (exit 0). The Bats suites exercise the real helper and prompt-flow contracts with direct state/content assertions; the lone `fake-core` fixture value is test data, not a mock-only dependency.
- Verification V6: FAIL — `bats tests/*.bats` passed 342/343 but `tests/interview-framework.bats` still asserts the Claude plugin and marketplace version is `4.12.0`; release metadata is `4.12.1`. The chained parity, JSON, and whitespace checks did not run after that non-zero Bats exit. Test-quality inspection found direct helper/state and prompt-content assertions, not mock-only tests; `fake-core` is fixture data.

## Execution Log

- 2026-08-31 — Task 1.1: Added optional, shared `approvalGate` and append-only `approvalAudit` schema fields for spec and epic state. Revision gates require nonblank feedback. Verified JSON validity and byte-for-byte schema parity.
- 2026-08-31 — Task 1.2: Added the mirrored, locked `resolve-approval` command. It resolves one valid state-backed action, appends its audit only after an exact affirmative, and otherwise returns clarification without writing state.
- 2026-08-31 — V1: Verified phase-gate regression coverage (29/29) and mirrored helper/schema parity.
- 2026-08-31 — Task 1.3: Routed Claude's shared final and artifact approval contracts through the one-action resolver fallback, retaining canonical confirmation and delegation checks. Verified `bats tests/claude-phase-flow.bats` (25/25).
- 2026-08-31 — Task 1.4: Routed Codex's shared final and artifact approval contracts through the one-action resolver fallback, retaining canonical confirmation, delegation, descriptor, and audit boundaries. Verified `bats tests/codex-phase-flow.bats` (16/16).
- 2026-08-31 — V2: Verified `bats tests/claude-phase-flow.bats tests/codex-phase-flow.bats` (41/41, exit 0). Test-quality check passed: the Bats suites use direct content assertions and real hook invocations, with no mocks or mock-only assertions.
- 2026-08-31 — Task 2.1: Aligned Claude start, triage, and research handoffs with the shared one-action resolver while preserving canonical confirmations, delegation checks, and descriptor-free multi-option reviews. Verified `bats tests/claude-phase-flow.bats` (25/25).
- 2026-08-31 — Task 2.2: Aligned Claude requirements, design, and tasks handoffs with the shared one-action resolver while keeping multi-option walkthroughs descriptor-free, requiring recorded revision feedback, and retaining existing continuation and fresh-writer routes. Verified `bats tests/claude-phase-flow.bats` (25/25).
- 2026-08-31 — V3: Verified Claude handoff coverage: `bats tests/claude-phase-flow.bats` passed 25/25, and all six phase commands retain the shared gate contract.
- 2026-08-31 — Task 2.3: Documented Codex helper-owned `approvalGate`/`approvalAudit` state, the one-action resolver fallback, and descriptor-free multi-choice start review menu while retaining quick, confirmation, delegation, continuation, and fresh-writer routes.
- 2026-08-31 — Task 2.4: Aligned Codex triage, research, and requirements handoffs with the resolver fallback, current descriptor requirement, recorded revision feedback, and existing delegation/writer routes. Verified `bats tests/codex-phase-flow.bats` (16/16).
- 2026-08-31 — V4: Verified Codex early-handoff coverage: `bats tests/codex-phase-flow.bats` passed 16/16, including shared-gate, ungated-write, and unique-writer routing assertions.
- 2026-08-31 — Task 2.5: Aligned Codex design and tasks handoffs with descriptor-free multi-option reviews, one-action resolver fallback, recorded revision feedback, and existing fresh dispatch routes. Verified `bats tests/codex-phase-flow.bats` (16/16).
- 2026-08-31 — Task 3.1: Added Bats coverage for accepted pre-delegation, artifact, revision, audit, live-descriptor resume, and no-mutation rejection behavior across both helpers. Verified `bats tests/phase-gates.bats` (31/31), helper/schema parity, and `git diff --check`.
- 2026-08-31 — Task 4.1: Bumped only the Claude plugin, marketplace entry, and Codex plugin manifest to 4.12.1. Verified all required release metadata agrees and JSON remains valid.
- 2026-08-31 — Task 3.2: Added focused Claude and Codex prompt-flow checks for the shared resolver fallback, helper-owned one-descriptor state, recorded revision feedback, canonical confirmation/delegation, and the 4.12.1 Codex manifest. Verified `bats tests/claude-phase-flow.bats tests/codex-phase-flow.bats` (43/43).
- 2026-08-31 — V5: Verified `bats tests/phase-gates.bats tests/claude-phase-flow.bats tests/codex-phase-flow.bats` (74/74, exit 0). Direct helper/state and prompt-content assertions cover the accepted, rejected, ambiguity, audit, and resume seams without mock-only dependencies.
- 2026-08-31 — Task 4.2: Updated the final stale `4.12.0` release assertion to `4.12.1`. Verified `bats tests/interview-framework.bats` (18/18, exit 0).
- 2026-08-31 — V6: Verified `bats tests/*.bats` (343/343, exit 0), helper/schema parity, all three release JSON manifests, and `git diff --check`. The test-generated `specs/.index` byproducts were restored before completion.

## Completed Tasks

- [x] 1.1 Define the persisted one-action approval model
- [x] 1.2 Add the shared state-aware approval resolver
- [x] V1 POC shared-gate checkpoint
- [x] 1.3 Route Claude's shared gate contract through the resolver - fd6df62
- [x] 1.4 Route Codex's shared gate contract through the resolver
- [x] V2 Shared-contract checkpoint
- [x] 2.1 Align Claude start, triage, and research handoffs
- [x] 2.2 Align Claude downstream artifact handoffs
- [x] V3 Claude handoff checkpoint
- [x] 2.4 Align Codex triage, research, and requirements handoffs
- [x] V4 Codex early-handoff checkpoint
- [x] 2.5 Align Codex design and tasks handoffs
- [x] 3.1 Add resolver behavior and state-safety coverage
- [x] 4.1 Bump the affected plugin release metadata (prerequisite for 3.2)
- [x] 3.2 Add Claude and Codex prompt-flow contract coverage
- [x] V5 Contextual-approval test checkpoint
- [x] 4.2 Update the remaining release-version assertion
- [x] V6 Local release-quality gate

## Current Task

All 19 tasks complete; coordinator should clean up the local state file.
