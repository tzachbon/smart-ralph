# Progress: tdd-bug-fix-pattern

## Goal

Add a dedicated bug fix pattern/workflow to Ralph Specum. When a user requests a bug fix (not a feature), Ralph should follow a specific sequence:
1. Understand the bug
2. Replicate it / capture logs / document the BEFORE state
3. Research root cause and best fix approach online
4. Suggest and implement a proper fix using TDD (red-green-refactor)

This involves updating Ralph's phases to detect "fix" intent and applying bug-specific steps, drawing from industry best practices for debugging and TDD-based bug fixing.

## Skill Discovery
- **communication-style**: no match
- **delegation-principle**: no match
- **interview-framework**: matched (keywords: phases, understand)
- **reality-verification**: matched (keywords: fix, fixes, reproduce, bug)
- **smart-ralph**: no match
- **spec-workflow**: matched (keywords: research, phases)

## Completed Tasks
- [x] 1.1 Add BUG_FIX intent to intent-classification.md - b8450df
- [x] 1.2 Add Bug Interview section to goal-interview.md - a6111bb
- [x] 1.4 Add Bug TDD Workflow to phase-rules.md - a13fbba
- [x] 1.5 [VERIFY] Quality checkpoint: phase-rules.md integrity - PASS (BUG_FIX count >= 2)
- [x] 1.6 Flesh out step 10 in quick-mode.md - pending commit
- [x] 1.7 Add Bug TDD Task Planning section to task-planner.md - pending commit
- [x] 1.8 [VERIFY] Quality checkpoint: all 5 files modified - PASS (all 5 grep checks passed)
- [x] 2.1 Bump plugin version in manifest files - 4.8.3 -> 4.8.4

## Current Task
Awaiting next task

## Learnings
- Ralph already has partial bug fix infrastructure: `VF` task template in phase-rules.md, `goalType: "fix"` detection in quick-mode.md, `[RED]/[GREEN]/[YELLOW]` tags in spec-executor.md
- The gap is no `BUG_FIX` intent type -- bug goals fall into TRIVIAL or MID_SIZED, bypassing reproduce-first enforcement
- `quick-mode.md` step 10 ("For fix goals: run reproduction, document BEFORE state") is a stub with no implementation detail
- `## Reality Check (BEFORE)` format is referenced in VF task template but never defined canonically
- Phase-rules.md TDD workflow has no Phase 0 (reproduce) -- just jumps to RED test
- Recommended change set: 6 files, all additive (new `BUG_FIX` routing branch, no breaking changes)
- Industry consensus: reproduce first, write test capturing bug, minimal fix, VF to verify -- exactly matches Ralph's existing TDD + VF primitives
- Goal interview needs 5 new bug-specific questions (repro steps, expected vs actual, when it started, regression check)
- Requirements scope decision: `spec-executor.md` NOT touched -- RED/GREEN/YELLOW already works; VF task template NOT touched -- format already correct
- Canonical BEFORE state format defined in requirements FR-4 -- must be added to `phase-rules.md`, not invented per-agent
- Unresolved: quick mode reproduction command source when goal text lacks explicit command (recommendation: run existing tests to find a failure)
- Unresolved: whether parallel research teammates model works for bugs or whether serial (reproduce -> root cause search) is needed
- 6 file touchpoints confirmed: intent-classification.md, phase-rules.md, quick-mode.md, goal-interview.md, task-planner.md (5 modifications + canonical BEFORE format addition to phase-rules.md)
- Design decision: TRIVIAL-specific keywords (typo, spelling, minor, tiny, rename) override BUG_FIX when both match -- "fix typo" -> TRIVIAL, "fix login bug" -> BUG_FIX
- Design decision: parallel research model kept (no serial change needed) -- Phase 0 reproduction happens in task execution, not research phase
- Design decision: quick mode fallback command priority: (1) goal text backtick/run pattern, (2) pnpm/npm/yarn test, (3) skip with warning
- Design decision: Phase 0 always exactly one [VERIFY] task (0.1) -- not variable; BEFORE format canonical in phase-rules.md
- Design decision: if existing test already failing (Q4), task-planner uses it as the [RED] task instead of writing new one
- Resolved: parallel research works for bugs; serial reproduce-first is enforced at task level (Phase 0), not research phase
- Revision 2 decision: AC-1.3 updated to min 5, max 5 (fixed count) -- bug interview is fully structured, all 5 questions mandatory; no depth-adaptive logic needed
- Revision 2 decision: Phase 0 expanded to two tasks (0.1 run+capture, 0.2 consistency re-run) per AC-3.3 requirement for at least two runs; RED task cannot start until both pass
- Task planning: coarse granularity chosen (15 tasks total) -- markdown-only spec with no compilation step; grep-based verification throughout
- Task planning: VF task checks for "Confirmed failing" field in phase-rules.md as proxy for canonical BEFORE format being present
- Task planning: plugin version bump task (2.1) included per CLAUDE.md requirement -- patch bump required for any plugin file change
- Task planning: parallel [P] markers on 1.1 and 1.2 only (no file overlap); all other tasks sequential due to dependencies or [VERIFY] nature
- Task planning: no VE tasks generated -- this is a markdown plugin with no dev server, no test runner; build verification is JSON manifest parsing only
