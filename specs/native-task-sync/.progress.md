# Progress: native-task-sync

## Goal
Enhance the implement command to sync tasks.md entries as native Claude Code tasks (TaskCreate/TaskUpdate), loaded in batches of ~10 to handle large task lists, keeping both sources of truth (tasks.md and native tasks) in sync.

## Completed Tasks
- [x] 1.1 Add native sync state fields to implement.md
- [x] 1.2 Add batch push strategy to coordinator-pattern.md
- [x] 1.3 Add Initial Setup sync section to coordinator-pattern.md

## Current Task
Awaiting next task

## Learnings
- The jq merge pattern in implement.md Step 3 uses `. + { ... }` to merge new fields into existing state. New fields go at the end of the object.
- Backwards compatibility note must be updated when adding new state fields so missing fields get defaults.
- Three locations need updating: example JSON block, jq merge pattern, and backwards compatibility note.
- Git Push Strategy section goes between State Update and Progress Merge in coordinator-pattern.md.
- Stop-hook Resume Critical section is where operational reminders go (batch push, verification layers, etc.).
- Native Task Sync sections insert between existing coordinator sections. Initial Setup goes after "Read State" error block (line 43) and before "Check Completion".

## Next
Task 1.4: Add Pre-Delegation and Post-Verification sync sections

## Interview Format
- Version: 1.0

## Intent Classification
- Type: MID_SIZED
- Confidence: high (4 keywords matched)
- Min questions: 3
- Max questions: 7
- Keywords matched: implement, tasks, sync, command

## Interview Responses

### Goal Interview (from start.md)
- **Batching strategy**: Rolling window - as each task completes, slide forward (always show next 9 upcoming + 1 sync)
- **Source of truth**: Bidirectional sync - either tasks.md or native tasks can update, the other syncs
- **Task detail level**: Title + first 1-2 sub-items for context
- **Sync location**: Both coordinator and stop-hook call a shared sync utility
- **Completed task visibility**: Research best practices (TBD from research phase)
- **Autonomy constraint**: Implementation phase MUST be fully autonomous - zero user interaction, no approval prompts, no manual steps during the execute loop. The sync layer must work without any user input.
- **Chosen approach**: B: Sync-layer script - shared utility called by coordinator and stop-hook for consistent bidirectional sync
- Spec location: ./specs/ (single configured directory)

### Requirements Interview (from requirements.md)
- **Priority**: Comprehensive - full sync across all 8 integration points including failures, modifications, parallel batches
- **Window size**: NO WINDOW NEEDED - user confirmed Claude Code handles 67+ tasks easily. Create ALL tasks upfront, no batching/windowing.
- **Chosen approach**: Comprehensive with full upfront task creation (simplified - no rolling window)

### Tasks Interview (from tasks.md)
- **Granularity**: Coarse (10-20 larger tasks)
- **E2E verification**: NO - manual testing only
- **Chosen approach**: Coarse POC-first - fewer tasks, ship fast, 3 files to modify

### Design Interview (from design.md)
- **Architecture**: Inline in coordinator-pattern.md - add sync sections directly into existing coordinator flow. No new files.
- **Init strategy**: All at once - create all tasks in coordinator's first turn. Simple and complete.
- **Chosen approach**: Inline sync in coordinator-pattern.md, all tasks created upfront

## Skill Discovery
- **Command Development** (.agents/skills): matched (reason: modifying implement command)
- **Hook Development** (.agents/skills): matched (reason: stop-hook controls execution loop)
- **Plugin Structure** (.agents/skills): no match
- **Skill Development** (.agents/skills): no match
- **MCP Integration** (.agents/skills): no match
- **Plugin Settings** (.agents/skills): no match
