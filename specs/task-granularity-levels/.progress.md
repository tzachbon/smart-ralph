# Progress: task-granularity-levels

## Original Goal
Add configurable task granularity levels (fine/coarse/auto) to ralph-specum's task planner, reducing over-granularity for sequential execution. Also convert verification checkpoints from discrete tasks to automatic hooks. Source: https://github.com/tzachbon/smart-ralph/issues/99

## Interview Format
- Version: 1.0

## Intent Classification
- Type: GREENFIELD
- Confidence: medium (2 keywords matched)
- Min questions: 5
- Max questions: 10
- Keywords matched: add, configurable

## Skill Discovery
- **Hook Development** (.agents/skills): matched (reason: issue requests converting verification checkpoints to hooks)
- **Plugin Settings** (.agents/skills): matched (reason: granularity level needs per-project configuration)
- **Command Development** (.agents/skills): matched but invocation failed (reason: --granularity flag on tasks command)
- **MCP Integration** (.agents/skills): no match
- **Plugin Structure** (.agents/skills): no match
- **Skill Development** (.agents/skills): no match

## Interview Responses

### Goal Interview (from start.md)
- Scope: All three granularity levels (fine, coarse, auto) in v1
- Auto logic: Fine if parallel dispatch plugin available, coarse if sequential execution
- Verification: [VERIFY] tasks remain opt-in, only generated in fine mode
- Default: Ask during interview with fine pre-selected (user prefers many tasks)
- Flag scope: --granularity on /ralph-specum:tasks + /ralph-specum:start (start passes through)
- Chosen approach: Extend task-planner agent to support granularity parameter, add interview question, auto-detect parallel plugin

### Requirements Interview (from requirements.md)
- Primary users: Plugin users running /ralph-specum:tasks
- Priority: Full feature set (fine + coarse + interview question + flag)
- Out of scope: Auto-detection logic, plugin settings (.local.md)
- Success criteria: Task count matches level (fine=40-60+, coarse=10-20)
- [P] markers: Yes, keep in both modes (independent of granularity)
- Chosen approach: Full feature -- fine + coarse with flag on /start + /tasks, interview question, [VERIFY] opt-in in fine mode only

## Learnings
- Auto-detection logic explicitly deferred by user -- only fine + coarse in scope
- Plugin settings (.local.md) also deferred -- no default_granularity config file
- Coarse mode still needs [P] markers -- granularity and parallelism are orthogonal concerns
- Final verification sequence (V4-V6) must always be generated regardless of mode -- non-negotiable quality gate
- TDD workflow coarse task counts unclear -- current TDD minimum is 30+, coarse TDD may need 8-15 range (flagged as unresolved)
- task-planner.md "Task Sizing Rules" section (lines 510-541) is the primary extension point -- sizing rules become conditional on granularity
- Flag parsing follows existing pattern in commands (string matching on $ARGUMENTS) -- no new parsing infrastructure needed
- VE tasks independent of granularity -- generated in both modes per project type detection

### Design Interview (from design.md)
- Architecture: Separate reference file (references/sizing-rules.md) with per-level rules, referenced from task-planner.md
- Validation: Fail silent -- invalid --tasks-size value defaults to fine with warning
- Chosen approach: New reference file for sizing rules, conditional reference in task-planner.md, flag parsing in commands

## Learnings
- Auto-detection logic explicitly deferred by user -- only fine + coarse in scope
- Plugin settings (.local.md) also deferred -- no default_granularity config file
- Coarse mode still needs [P] markers -- granularity and parallelism are orthogonal concerns
- Final verification sequence (V4-V6) must always be generated regardless of mode -- non-negotiable quality gate
- TDD workflow coarse task counts unclear -- current TDD minimum is 30+, coarse TDD may need 8-15 range (flagged as unresolved)
- task-planner.md "Task Sizing Rules" section (lines 510-541) is the primary extension point -- sizing rules become conditional on granularity
- Flag parsing follows existing pattern in commands (string matching on $ARGUMENTS) -- no new parsing infrastructure needed
- VE tasks independent of granularity -- generated in both modes per project type detection
- Design: Only 8 files need changes (1 new, 7 modified) -- change is well-localized
- Design: task-planner.md hardcoded section replacement is the riskiest change -- sizing-rules.md reference must be bulletproof
- Design: The `granularity` field is intentionally NOT in the `required` array of spec.schema.json for backwards compat
- Design: No changes needed to stop-watcher.sh, spec-executor.md, or hook infrastructure
- Design: TDD coarse count set to 8-15 (proportional from fine's 30-50) -- may need tuning after first real usage
- Design: Previous design.md was stale (3 levels, wrong flag name, wrong counts) -- completely replaced with requirements-aligned version
- Flag parsing: start.md and tasks.md --tasks-size handling already consistent -- same $ARGUMENTS pattern, identical warning message, same `granularity` field name. Absent-flag behavior differs intentionally (start omits, tasks preserves existing)

### Tasks Interview (from tasks.md)
- E2E verification: NO -- markdown-only plugin changes, no dev server/runtime
- Execution: Parallel -- maximize [P] markers for independent file changes
- Chosen approach: Parallel execution -- independent files get [P] markers, core reference file first, then parallel command/schema changes

## Completed Tasks
- [x] 1.1 Create sizing-rules.md reference file
- [x] 1.2 Replace hardcoded sizing section in task-planner.md
- [x] 1.3 Update quality checklist in task-planner.md
- [x] 1.8 Update tasks.md frontmatter and flag parsing
- [x] 1.9 Add granularity interview question to tasks.md
- [x] 1.10 Add granularity to tasks.md delegation context
- [x] 1.11 POC Checkpoint - all 9 files verified
- [x] 2.1 Ensure sizing-rules.md formatting matches existing references - no changes needed, already consistent
- [x] 2.2 Verify task-planner.md conditional reference is clear - no changes needed, reference path and instructions already clear
- [x] 2.3 Verify flag parsing consistency across start.md and tasks.md - no changes needed, already consistent
- [x] 3.1 Validate schema JSON is well-formed - PASS (verification only, no changes needed)
- [x] 3.2 Validate sizing-rules.md content completeness - PASS (verification only, no changes needed)
- [x] 3.3 Validate all AC references are addressable - PASS (verification only, no changes needed)
- [x] 4.1 [P] Bump plugin.json version 4.4.0 to 4.5.0
- [x] 4.2 [P] Bump marketplace.json version 4.4.0 to 4.5.0
- [x] V4 [VERIFY] Full local quality check - ALL PASS
- [x] V5 [VERIFY] CI pipeline passes (deferred to PR creation)
- [x] V6 [VERIFY] AC checklist - ALL 31 ACs PASS
- [x] 5.1 Create PR and verify CI - PR #109 created, all CI green

## Current Task
Awaiting next task

## Next
5.2 Address review comments

## Task Planning Learnings
- 22 total tasks across 5 phases: 11 POC, 3 refactoring, 3 testing, 5 quality gates, 2 PR lifecycle
- Tasks 1.2+1.3 both modify task-planner.md (different sections) -- must be sequential, NOT parallel
- Tasks 1.8/1.9/1.10 all modify tasks.md command -- must be sequential
- Parallel group 1: tasks 1.4+1.5+1.6+1.7 (intent-classification.md, spec.schema.json, start.md, CLAUDE.md -- zero overlap)
- Parallel group 2: tasks 4.1+4.2 (plugin.json, marketplace.json -- zero overlap)
- No intermediate [VERIFY] tasks -- no lint/typecheck/build commands for markdown plugin files
- Testing phase uses structural grep/python verification instead of test framework -- appropriate for markdown-only changes
- Version bump deferred to Phase 4 (quality gates) to avoid re-bumping if POC changes needed
---
# Progress: task-granularity-levels (Task 3 - parallel)

## Completed Tasks
- [x] 1.4 [P] Add --tasks-size to intent-classification.md

## Learnings
- intent-classification.md argument parsing list follows simple bullet pattern with bold flag name and description
- Examples section uses arrow notation for intent mapping (e.g., `-> Coarse granularity`)
---
# Progress: task-granularity-levels (Task 4 - parallel)

## Completed Tasks
- [x] 1.5 [P] Add granularity field to spec.schema.json

## Learnings
- macOS does not have flock; parallel locking needs alternative approach (shlock or no-op on darwin)
- granularity field added to definitions.state.properties as optional (not in required array) for backwards compatibility
---
## Completed Tasks
- [x] 1.6 [P] Update start.md frontmatter and flag parsing

## Learnings
- start.md state init JSON uses a post-block instruction pattern for conditional fields (rather than inline conditionals in JSON) -- keeps the JSON template clean while documenting the --tasks-size handling logic
- Flag parsing follows established pattern: flags list in Step 2 summary, conditional state field in Step 4 New Flow item 7
---
## Completed Tasks
- [x] 1.7 [P] Document --tasks-size flag in CLAUDE.md

## Learnings
- CLAUDE.md Development section has a clear insertion point between the test workflow code block and the version bump warning blockquote
- tasks.md Step 1 items are numbered sequentially; --tasks-size handling inserted as item 7 (after reading .ralph-state.json), bumping "Read context" to item 8
---
# Progress: task-granularity-levels (Task 17 - parallel)

## Completed Tasks
- [x] 4.1 [P] Bump plugin.json version 4.4.0 to 4.5.0

## Learnings
- plugin.json version bump is straightforward single-field edit; no other fields depend on version string
---
# Progress: task-granularity-levels (Task 18 - parallel)

## Completed Tasks
- [x] 4.2 [P] Bump marketplace.json version 4.4.0 to 4.5.0

## Learnings
- marketplace.json has a single ralph-specum entry at the top of the plugins array; version field update is straightforward

### Verification: V4 [VERIFY] Full local quality check
- Status: PASS
- All 9 target files exist with expected patterns
- Schema JSON valid, granularity field correct (type=string, enum=[fine,coarse], not in required)
- plugin.json and marketplace.json both at 4.5.0 (match)
- No unintended file changes: only 9 target files + spec artifacts modified
- Changed files outside spec dir: marketplace.json, CLAUDE.md, plugin.json, task-planner.md, start.md, tasks.md, intent-classification.md, sizing-rules.md, spec.schema.json (all expected)

### Verification: V6 [VERIFY] AC checklist
- Status: PASS
- All 31 acceptance criteria verified across 8 user stories
- AC-1.1-1.4 (start.md flag parsing): PASS -- stores granularity, omits when absent, warns on invalid
- AC-2.1-2.4 (tasks.md flag parsing): PASS -- stores, overrides start value, warns on invalid
- AC-3.1-3.5 (fine sizing): PASS -- 40-60+ tasks, max 4 steps, max 3 files, [VERIFY] every 2-3, V4-V6 always
- AC-4.1-4.6 (coarse sizing): PASS -- 10-20 tasks, max 8-10 steps, max 5-6 files, no intermediate [VERIFY], V4-V6 always, single logical concern
- AC-5.1-5.5 (interview question): PASS -- asks when absent, fine recommended, skips when set, stores response, quick defaults to fine
- AC-6.1-6.4 (state field): PASS -- schema has granularity enum, not required (backwards compat), task-planner reads it, re-run updates
- AC-7.1-7.3 ([P] markers): PASS -- both modes generate [P], [VERIFY] breaks groups
- AC-8.1 (CLAUDE.md docs): PASS -- --tasks-size documented with examples and explanations
