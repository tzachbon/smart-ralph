interface:
  display_name: "Ralph Specum Tasks"
  short_description: "Generate implementation tasks for an active spec"
  default_prompt: "Use $ralph-specum-tasks to run bundled grill-with-docs behavior, write tasks.md, then ask me to `continue to implementation`, `run review agent`, or `request changes`."
policy:
  allow_implicit_invocation: false
