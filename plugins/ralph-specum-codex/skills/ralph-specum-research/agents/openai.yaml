interface:
  display_name: "Ralph Specum Research"
  short_description: "Research, then route requirements or a prototype"
  default_prompt: "Use $ralph-specum-research to write research.md and offer `continue to requirements`, `continue to prototype`, or `request changes`; route prototype work through $ralph-specum-prototype."
policy:
  allow_implicit_invocation: false
