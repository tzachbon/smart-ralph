interface:
  display_name: "Ralph Specum Start"
  short_description: "Create or resume a Ralph spec"
  default_prompt: "Use $ralph-specum-start to create or resume a Ralph spec with progress.md as durable state, then wait unless I explicitly request autonomous execution."
policy:
  allow_implicit_invocation: false
