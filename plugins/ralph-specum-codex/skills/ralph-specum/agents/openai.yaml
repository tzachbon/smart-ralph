interface:
  display_name: "Ralph Specum"
  short_description: "Run Ralph Specum workflows in Codex"
  default_prompt: "Use $ralph-specum to run Ralph step by step in this repo. In normal mode, use bundled grill-with-docs behavior, then offer continue, `run review agent`, `run prototype` when available, or `request changes`."
policy:
  allow_implicit_invocation: false
