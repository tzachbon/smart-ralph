interface:
  display_name: "Ralph Specum"
  short_description: "Run Ralph Specum workflows in Codex"
  default_prompt: "Use $ralph-specum to coordinate this spec workflow with native Codex subagents. Use /goal only when I explicitly request autonomous execution."
policy:
  allow_implicit_invocation: false
