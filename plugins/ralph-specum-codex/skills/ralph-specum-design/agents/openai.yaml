interface:
  display_name: "Ralph Specum Design"
  short_description: "Design with approved prototype evidence"
  default_prompt: "Use $ralph-specum-design to select valid prototype evidence before writing design.md; route active prototype blockers through $ralph-specum-prototype, then ask for the next design decision."
policy:
  allow_implicit_invocation: false
