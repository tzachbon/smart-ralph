interface:
  display_name: "Ralph Specum Status"
  short_description: "Show active spec and backlog status"
  default_prompt: "Use $ralph-specum-status to show Ralph artifact, checkbox, progress.md, and native goal status without changing anything."
policy:
  allow_implicit_invocation: false
