interface:
  display_name: "Ralph Specum Triage"
  short_description: "Split a large effort into dependency-aware specs"
  default_prompt: "Use $ralph-specum-triage with bounded read-only native subagents to produce a dependency-aware epic and next unblocked spec."
policy:
  allow_implicit_invocation: false
