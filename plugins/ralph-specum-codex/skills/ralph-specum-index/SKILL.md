---
name: ralph-specum-index
description: Compatibility shim for the deprecated Ralph Specum index skill. Use only when the user explicitly invokes `$ralph-specum-index`.
---

# Ralph Specum Index Compatibility Shim

Warn that `$ralph-specum-index` is deprecated in v5 and removed in v6. Load `../ralph-specum/SKILL.md` relative to this file, then route the request to `$ralph-specum` with the intent `build or refresh repository index artifacts`.
