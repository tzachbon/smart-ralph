interface:
  display_name: "Ralph Specum Requirements"
  short_description: "Requirements, then route design or a prototype"
  default_prompt: "Use $ralph-specum-requirements to write requirements.md and offer `continue to design`, `continue to prototype`, or `request changes`; route prototype work through $ralph-specum-prototype. The skill contract owns the single autonomous quick gate."
policy:
  allow_implicit_invocation: false
