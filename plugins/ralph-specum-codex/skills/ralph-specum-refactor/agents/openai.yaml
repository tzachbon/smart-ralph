interface:
  display_name: "Ralph Specum Refactor"
  short_description: "Update specs after implementation learnings"
  default_prompt: "Use $ralph-specum-refactor to reconcile prototype recovery, pause only blocked or stale spec files, restore returnTaskIndex when needed, then ask me to `approve current artifact`, `request changes`, or `continue to implementation`."
policy:
  allow_implicit_invocation: false
