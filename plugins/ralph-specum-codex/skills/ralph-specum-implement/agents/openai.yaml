interface:
  display_name: "Ralph Specum Implement"
  short_description: "Implement one batch or start a native goal"
  default_prompt: "Use $ralph-specum-implement to implement one verified logical batch. Use native /goal only if I explicitly request autonomous execution."
policy:
  allow_implicit_invocation: false
