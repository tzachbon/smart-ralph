interface:
  display_name: "Ralph Specum Tasks"
  short_description: "Plan tasks with approved prototype evidence"
  default_prompt: "Use $ralph-specum-tasks to select valid prototype evidence before writing tasks.md; route active prototype blockers through $ralph-specum-prototype, then ask for the next implementation decision."
policy:
  allow_implicit_invocation: false
