interface:
  display_name: "Ralph Specum Tasks"
  short_description: "Generate implementation tasks for an active spec"
  default_prompt: "Use $ralph-specum-tasks with bounded read-only native subagents, write a verifiable tasks.md as the root coordinator, and stop for approval."
policy:
  allow_implicit_invocation: false
